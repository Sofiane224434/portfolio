services:
  react-app:
    build: .
    container_name: portfolio_app
    restart: unless-stopped
    ports:
      - "127.0.0.1:3002:80"
    networks:
      - web

networks:
  web:
    external: true
