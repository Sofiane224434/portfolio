# ═══════════════════════════════════════════════════════════════════════
# WORKFLOW DE DÉPLOIEMENT AUTOMATIQUE - REACT + VITE
# ═══════════════════════════════════════════════════════════════════════
name: Deploy React App to VPS

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Étape 1 : Récupérer le code
      - name: Checkout code
        uses: actions/checkout@v4

      # Étape 2 : Se connecter au VPS et déployer
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            set -e

            APP_DIR=~/apps/deploy-app-react
            REPO_URL=https://github.com/${{ github.repository }}.git

            echo "🌐 Vérification du réseau Docker..."
            docker network inspect web >/dev/null 2>&1 || docker network create web

            echo "📁 Préparation du dossier..."
            mkdir -p $APP_DIR
            cd $APP_DIR

            echo "📥 Récupération du code..."
            if [ -d ".git" ]; then
              git fetch origin
              git reset --hard origin/main
              git clean -fd
            else
              rm -rf $APP_DIR/*
              rm -rf $APP_DIR/.*  2>/dev/null || true
              git clone $REPO_URL .
            fi

            echo "🔍 Vérification du code récupéré..."
            echo "Dernier commit:"
            git log -1 --oneline
            echo "Contenu de Projects.jsx:"
            grep -A 2 "title:" src/components/Projects.jsx || echo "Fichier non trouvé"

            echo "🐳 Build Docker..."
            docker compose down || true
            docker rm -f portfolio_app || true
            docker rmi deploy-app-react-react-app || true
            docker compose build --no-cache

            echo "🚀 Lancement du container..."
            docker compose up -d

            echo "🔍 Vérification du contenu déployé..."
            docker exec portfolio_app cat /usr/share/nginx/html/index.html

            echo "🧹 Nettoyage..."
            docker image prune -f

            echo "✅ Déploiement terminé !"
            docker ps | grep portfolio_app
